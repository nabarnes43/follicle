# Follicle v0.0

AI-powered hair care recommendation engine.

## Setup

```bash
npm install
npm run dev
```

# Delete the cache directories
rm -rf .next
rm -rf node_modules/.cache

# Then rebuild
npm run build