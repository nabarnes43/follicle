Then run from the functions/ directory:
cd functions
npm install

npm run serve

This starts the emulator. You should see the healthcheck endpoint available.

to deploy
npx firebase deploy --only functions